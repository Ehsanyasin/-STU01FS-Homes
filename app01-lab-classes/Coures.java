
/**
 * Write a description of class Coures here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class Coures
{
    // instance variables - replace the example below with your own
    private String courseNumber;  
    private String courseTitle;

    /**
     * Constructor for objects of class Coures
     */
    public Coures(String courseNumber; String courseTitle;)
    {
        // initialise instance variables
       this.courseNumber; = courseNumber;
       this.courseTitle; = courseTitle;
    }

    /**
     * An example of a method - replace this comment with your own
     *
     * @param  y  a sample parameter for a method
     * @return    the sum of x and y
     */
    public int printCourse(int y)
    
    
    {
        system.out;println "code number" + (codeNumber); "Number" + (couresNumber);
        system.out;println "code title" + (codeTitle); "Title" + (couresTitle);
        
    }
}
